"""Render helper functions."""

def find_template(path=None, experiment=None, mac_name=None):
    """
    Find system path (absolute) to mac templates.
    """
    if path is None:
        
    if path is not None:

