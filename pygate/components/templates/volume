{% from "vec3" import vec3 -%}
{% macro volume(o) -%}
/gate/{{ o.mother }}/daughters/name            {{ o.name }}
/gate/{{ o.mother }}/daughters/insert          {{ o.shape_type}}
{% block size scoped -%}
{%- endblock -%}
/gate/{{ o.name }}/placement/setTranslation    {{ vec3(o.position) }} mm
{% if o.material -%}
/gate/{{ o.name }}/setMaterial                 {{o.material}}
{%- endif %}
{%- endmacro -%}
{% if o -%}
{{ volume(o) }}
{%- endif %}