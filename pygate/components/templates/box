{% extends "volume" %}
{% block size -%}
/gate/{{ o.name }}/geometry/setXLength         {{ o.size.x }} mm
/gate/{{ o.name }}/geometry/setYLength         {{ o.size.y }} mm
/gate/{{ o.name }}/geometry/setZLength         {{ o.size.z }} mm
{% endblock size -%}

