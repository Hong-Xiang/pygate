{% macro geometry(o) -%}
{{ o.world.render() }}
{{ o.camera.render() }}
{{ o.phantom.render() if o.phantom }}
{% for s in o.surfaces -%}
{{ s.render() }}
{% endfor -%}
{% endmacro -%}
{% if o -%}
{{ geometry(o) }}
{%- endif %}